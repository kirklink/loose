const dynamicNameToken = '^^^';

const authority = 'firestore.googleapis.com';

const apiVersion = 'v1';


const cloudPlatformScope = 'https://www.googleapis.com/auth/cloud-platform';
const datastoreScope = 'https://www.googleapis.com/auth/datastore';